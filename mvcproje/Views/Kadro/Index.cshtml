@model List<Kadro>

<h1>Futbolcu Kadrosu</h1>
<div class="row">
    
        @foreach(var item in Model){
            <div class="col-md-4">
                @await Html.PartialAsync("_Card",item)
            </div>
            
        }
    
</div>
